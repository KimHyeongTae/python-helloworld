{
  "version": 2,
  "builds": [
    {
      "src": "main.py",
      "use": "@now/python",
      "config": { "maxLambdaSize": "10mb" },
      "command": "pip install google-api-python-client -t ."
    }
  ],
  "routes": [
    { "src": "/.*", "dest": "main.py" }
  ]
}
